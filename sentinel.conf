# specify path to gentarium.conf or leave blank
# default is the same as GentariumCore
#gentarium_conf=/home/evan82/.gentariumcore/gentarium.conf

# valid options are mainnet, testnet (default=mainnet)
network=mainnet
#network=testnet

# database connection details
db_name=database/sentinel.db
db_driver=sqlite
